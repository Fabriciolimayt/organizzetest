# Basedash Reference Map For Organizze

The supplied Basedash video is 127.750 seconds long. The temporary AVFoundation extraction produced timestamped PNG frames in `/tmp/organizzze-basedash-frames`; these files are review material and are not repository assets. The supplied static page reference is 3840 x 20053 px.

The timestamps below are exact decoded frame times from that extraction. They identify the closest reference composition or transition for each Organizze scene; they do not authorize reuse of Basedash copy, logos, screenshots, database concepts, or branded imagery.

| Scene | Reference timestamp/frame | Organizze job | Geometry | Reveal order | Mobile adaptation |
| --- | --- | --- | --- | --- | --- |
| `prelude` | `00:04.250`, frame `01-004.250s.png` | establish product quality | layered dashboard above fold, partially occluded by centered type | shell, values, charts | stacked crop, no pin |
| `promise` | `00:00.000`, frame `00-000.000s.png` | explain consolidation and available amount | centered statement with a narrow measure and quiet black space | eyebrow, headline, copy, CTA | same order, shorter gap |
| `month` | `00:29.800`, frame `07-029.800s.png` | make one complete month inspectable | one wide product surface owns the stage, with dense internal bands | frame, tabs, primary values, charts, detail rows | stack summaries before detail; never shrink to a thumbnail |
| `whatsapp` | `00:59.617`, frame `14-059.617s.png` | turn a message or receipt into a financial decision | centered interpretation surface with subordinate context at the edges | input, interpretation, category, transaction, available amount | one-column pipeline with no fake chat shell or bubbles |
| `available` | `00:38.317`, frame `09-038.317s.png` | answer what remains available | dominant value region supported by a wide dashboard and compact evidence | available amount, calculation inputs, trend, explanation | lead with the amount, then stack calculation evidence |
| `sources` | `00:12.767`, frame `03-012.767s.png` | consolidate every expense source | source marks sit in a horizontal field above a single convergence point | source labels, connectors, ledger, total | vertical source flow into one owned ledger; no logo cloud |
| `plans` | `01:42.200`, frame `24-102.200s.png` | compare current plans without hiding the alternative | commercial statement bridges into a restrained, inspectable product stage | plan names, prices, eligibility, CTA | stacked comparison with both plans visible before CTA |
| `future` | `00:46.833`, frame `11-046.833s.png` | surface commitments and limits early | wide product stage with adjacent status columns and time-based rows | commitments, limits, warning state, next action | stack rows before limit tracks and reduce simultaneous reveals |
| `planning` | `01:16.650`, frame `18-076.650s.png` | connect budgets, goals, scenarios, and spaces | one bright focal panel sits within a field of related product surfaces | focal plan, related surfaces, progressive detail | preserve the focal-first order in a single column |
| `trust` | `01:33.683`, frame `22-093.683s.png` | explain privacy boundaries and system quality | a central system symbol anchors distributed evidence without card repetition | ownership, storage boundary, spaces, concise evidence | one-column evidence sequence with the symbol kept secondary to copy |
| `signature` | `02:07.733`, frame `30-127.733s.png` | close on the future month and Organizze brand | shallow-perspective chart enters above a near-viewport-width closing wordmark | graph, headline, CTA, wordmark | shallower depth, no pin, wordmark fitted without horizontal overflow |

**Typography scale.** The reference uses small utility navigation and eyebrow text against large centered editorial statements, then returns to compact product labels inside inspectable stages. Organizze keeps interface labels at 12 px or larger, public body copy at 15 px or larger, and dense product copy at 14 px or larger. Display text uses controlled line breaks and balanced width rather than viewport-width scaling.

**Hairlines.** Reference surfaces are structured by low-contrast 1 px rules, not heavy outlines or floating-card shadows. Organizze uses `#24272C` hairlines predominantly at 1 px, with 4-8 px panel radii.

**Product-surface depth.** Depth is created by occlusion, scale, perspective, panel contrast, controlled shadow, opacity, filter, and subtle raster texture. Light belongs to real interfaces and data states; decorative gradient blobs, bokeh, glassmorphism, and generic glow are excluded.

**Section spacing.** The reference alternates dense, large-format product stages with long quiet intervals. Public content stays near a 1180-1240 px maximum while stages may extend beyond that column without causing document overflow. Every first viewport must leave a visible hint of the following scene.

**Closing-brand scale.** The reference wordmark becomes the final monumental object and approaches the available content width beneath the perspective chart. Organizze follows that hierarchy without copying the Basedash mark: the original wordmark is the final large-format graphic, remains fully legible at 375 px, and never creates horizontal overflow.
